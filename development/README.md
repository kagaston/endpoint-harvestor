# Development

This directory contains tooling configuration for the IntrusionInspector workspace.

| File | Purpose |
|------|---------|
| `.pylintrc` | Pylint configuration for Python linting |
| `.yamllint.yaml` | YAML linting for profiles and rules |
| `.pre-commit-config-py.yaml` | Pre-commit hooks for Python files |
